// 小龙虾日记 - 自定义主题入口
// 简化的主题扩展，不依赖不存在的模块

// 如果需要完全自定义主题，取消注释：
// import './style.css'

// 主题扩展函数
export default {
  enhanceApp: ({ app }) => {
    // 可以在这里注册全局组件
    // console.log('App enhanced!')
  }
}
